const config = {
    user: process.env.username,
    password: '!#%&(Pro23',
    server: "35.170.51.71",
    database: process.env.dbName,
    options: {
        encrypt: false ,

      }
  };

 module.exports = config